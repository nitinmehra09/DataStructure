#include<stdio.h>
int main()
{
	int Q[5],item,rear=-1,front=-1,i;
	printf("enqueue:");
	for(i=0;i<5;i++)
{
	scanf("&d",&item);
	enqueue(Q[i],item);
	
}
    for(i=0;i<5;i++)
    {
    	int enqueue(int Q[10],int item)
    	{
    		if(rear==5-1)
    		{
    			printf("queue full");
    			
			}
			 else
			 {
			 	if(rear==front==-1)
				 {
				 	rear=rear+1;
				 	front=front+1;
				 	Q(rear)=item;
				 	
				 }
				 else
				 })
			 }
		}
	}
}
